{
	"version": 2,
	"functions": {
		"api/**/*.php": {
			"runtime": "vercel-php@0.7.4",
			"excludeFiles": "{.next,*.cache,.node_cache,.cursor,.ansible,node_modules,.venv,.direnv,public,reports/my-app,index/freebox/*.bmp}/**"
		}
	},
	"routes": [
		{ "src": "/api/(.*)", "dest": "/api/api/$1" },
		{ "src": "/ini/(.*)", "dest": "/api/ini/$1" },
		{ "src": "/ext/(.*)", "dest": "/api/ext/$1" },
		{ "src": "index/honeypot/(.*)", "dest": "/api/login/$1" },
		{ "src": "/(.*)", "dest": "/api/$1" }
	],
	"env": {
		"NOW_PHP_FOO": "bar3"
	},
	"build": {
		"env": {
			"NOW_PHP_DEBUG": "1"
		}
	}
}
