/**
 * Placeholder for optional Obs.js-style connection hints (see public/index.html comment).
 * Serves valid JavaScript so Next/static hosts never return HTML for this path (avoids
 * "expected expression, got '<'" when the real bundle is not shipped).
 */
void 0;
